# home

`HomePage`: the `/` route. Week select, hidden picks file input behind a button,
View Results (navigates to the week's scoreboard), Export Results, and the footer.
`HomePage.scss` carries the week select's whole look, since Base UI ships it
unstyled.
