# LogoButton

MUI Joy `Button` wrapping `logo192.png`, `onClick` prop. White 1px outline from an inline SVG `feMorphology` dilate filter, not stacked drop-shadows: a fixed dilation radius is even on every side, where sub-pixel shadow offsets rendered unevenly in Chrome.
